<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/primary_main"
    android:padding="8dp">

    <Button
        android:id="@+id/item_favorite_category_btn"
        android:layout_width="100dp"
        android:layout_height="120dp"
        android:background="@drawable/bg_category_btn_unselect"
        android:stateListAnimator="@null"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />
    
    <TextView
        android:id="@+id/item_favorite_category_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@string/category_new"
        android:textColor="@color/light_brown"
        android:textSize="14dp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="@id/item_favorite_category_btn"
        app:layout_constraintBottom_toBottomOf="@id/item_favorite_category_btn"
        app:layout_constraintStart_toStartOf="@id/item_favorite_category_btn"
        app:layout_constraintEnd_toEndOf="@id/item_favorite_category_btn" />
    
    <ImageView
        android:id="@+id/item_favorite_category_select_iv"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_category_select"
        app:layout_constraintTop_toTopOf="@id/item_favorite_category_btn"
        app:layout_constraintEnd_toEndOf="@id/item_favorite_category_btn"
        android:visibility="gone"/>

</androidx.constraintlayout.widget.ConstraintLayout>